# Machine Learning

Here, we include:

* Our trained MEGNet model in `models/model.hdf5`.
* Example structures, each in their own directory, for kL\_min prediction in `structures/`.
* An example script to perform predictions of kL\_min at 600 K for structures in `megnet_predict.py`.
